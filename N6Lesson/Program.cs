

DateTime time = DateTime.Now;

DateTime ustDan = DateTime.UtcNow;

DateTimeOffset dateTime=DateTimeOffset.Now;


Console.WriteLine(time.Year);

Console.WriteLine(ustDan.Kind);

Console.WriteLine(dateTime);